# iOS Pair Programming Task

App shows a list of comments with pagination (scroll to load more). Data from [JSONPlaceholder](https://jsonplaceholder.typicode.com) (no API key).

**Requirements:** Xcode + command line tools.
